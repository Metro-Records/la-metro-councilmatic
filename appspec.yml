version: 0.0
os: linux
files:
  - source: /
    destination: /home/datamade/la-metro-councilmatic
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/app_start.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 1000
      runas: root

branch_config:
  main:
    deploymentGroupName: staging

  deploy:
    deploymentGroupName: production
